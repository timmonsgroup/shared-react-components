{
  "name": "@timmons-group/shared-react-monorepo",
  "version": "0.0.0",
  "private": false,
  "eslintIgnore": [
    "node_modules",
    "build/*",
    "dist/*"
  ],
  "scripts": {
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ],
    "overrides": [
      {
        "rules": {
          "import/no-anonymous-default-export": "off"
        }
      }
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "peerDependencies": {
  }
}
